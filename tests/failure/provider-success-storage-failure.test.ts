import { ZStorageFixtureV1 } from '../../fixtures/v1/z-s.js';test('storage failure retains safe non-URL provider reference for storage-only retry',async()=>{const c=new ZStorageFixtureV1(),s=new AbortController().signal;await expect(c.completeOrIngestOutput({authorizationRef:'a',safeProviderOutputRef:'safe-ref',mimeType:'image/png',fixtureScenario:'storage-failure'},s)).rejects.toThrow(/storage/)})
