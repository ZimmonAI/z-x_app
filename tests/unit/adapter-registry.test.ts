import { getAdapter,listAdapters } from '../../src/adapters/registry.js';test('allowlists exactly four adapters and rejects unknown binding',()=>{expect(listAdapters().map(a=>`${a.id}@${a.version}`)).toEqual(['image-prompt-prepare-v1@1.0.0','image-generate-v1@1.0.0','scene-video-prompt-prepare-v1@1.0.0','scene-video-generate-v1@1.0.0']);expect(()=>getAdapter('image.generate.v1','bad','1.0.0')).toThrow(/allowlisted/)})
