import { buildServer } from '../../src/api/server.js';import { loadConfig } from '../../src/config.js';import { validRequest } from '../unit/test-request.js';const verify=async()=>({ownerApp:'video-maker',scopes:new Set(['zx.executions.submit','zx.executions.read','zx.executions.cancel','zx.executions.retry','zx.executions.reconcile']),payload:{}});test('authenticated API submit/read/cancel and owner isolation semantics',async()=>{const app=await buildServer({config:loadConfig({ZX_NODE_ENV:'test'}),verify});const h={authorization:'Bearer fixture'};const s=await app.inject({method:'POST',url:'/internal/v1/executions',headers:h,payload:validRequest()});expect(s.statusCode).toBe(202);const id=s.json().id;expect((await app.inject({method:'GET',url:`/internal/v1/executions/${id}`,headers:h})).statusCode).toBe(200);expect((await app.inject({method:'POST',url:`/internal/v1/executions/${id}/cancel`,headers:h})).statusCode).toBe(200);expect((await app.inject({method:'GET',url:`/internal/v1/executions/${id}`})).statusCode).toBe(401);await app.close()})
