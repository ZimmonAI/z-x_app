{
  "name": "@zimspace/z-x-execution-runner",
  "version": "0.1.0",
  "private": true,
  "description": "Neutral governed Z-X execution foundation.",
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "packageManager": "npm@10.9.2",
  "main": "dist/src/api/main.js",
  "exports": {
    "./contracts/v1": "./dist/src/contracts/v1/execution.js"
  },
  "files": [
    "dist",
    "migrations",
    "scripts",
    "docs",
    "zimspace.app.json",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:contract": "vitest run tests/contract",
    "test:integration": "vitest run tests/integration",
    "test:failure": "vitest run tests/failure",
    "validate": "npm run typecheck && npm run lint && npm run test:unit && npm run test:contract && npm run test:integration && npm run test:failure && npm run build && npm run db:migrate && npm run db:rollback && npm run package:validate && npm run manifest:validate",
    "dev:api": "npm run build && node --enable-source-maps dist/src/api/main.js",
    "dev:worker": "npm run build && node --enable-source-maps dist/src/worker/main.js",
    "start:api": "node --enable-source-maps dist/src/api/main.js",
    "start:worker": "node --enable-source-maps dist/src/worker/main.js",
    "start:auth-fixture": "node --enable-source-maps dist/src/auth-fixture/main.js",
    "fixture-auth:mint": "node --enable-source-maps dist/src/auth-fixture/mint.js",
    "worker:request-stop": "node --enable-source-maps dist/src/worker/request-stop.js",
    "db:migrate": "node scripts/run-migrations.mjs up",
    "db:rollback": "node scripts/run-migrations.mjs down",
    "package:validate": "node --input-type=module -e \"import p from './package.json' with {type:'json'};if(p.name!=='@zimspace/z-x-execution-runner'||p.type!=='module'||p.engines.node!=='>=24 <25'||p.main!=='dist/src/api/main.js'||p.exports['./contracts/v1']!=='./dist/src/contracts/v1/execution.js'||Object.keys(p.exports).length!==1||p.scripts['start:worker']!=='node --enable-source-maps dist/src/worker/main.js'||p.scripts['db:migrate']!=='node scripts/run-migrations.mjs up'||p.scripts['db:rollback']!=='node scripts/run-migrations.mjs down'||!p.files.includes('scripts')||p.files.some(x=>x.includes('.runtime')))process.exit(1);for(const [n,v] of Object.entries({...p.dependencies,...p.devDependencies})){if(!/^\\d+\\.\\d+\\.\\d+$/.test(v))throw new Error('dependency not exact: '+n+'='+v)}\"",
    "manifest:validate": "node scripts/validate-manifest.mjs"
  },
  "dependencies": {
    "@opentelemetry/api": "1.9.1",
    "fastify": "5.10.0",
    "jose": "6.2.3",
    "pg": "8.22.0",
    "pino": "10.3.1",
    "prom-client": "15.1.3",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "26.1.1",
    "@types/pg": "8.20.0",
    "eslint": "10.7.0",
    "typescript": "6.0.3",
    "typescript-eslint": "8.64.0",
    "vitest": "4.1.10"
  }
}
