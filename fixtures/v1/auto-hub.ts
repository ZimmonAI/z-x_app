import type { AutoHubDispatchClient } from '../../src/clients/auto-hub.js';import type { StartRunV1,GetRunV1,CancelRunV1,AutoHubRunV1 } from '../../src/contracts/v1/dependencies.js';import { SafeExecutionError } from '../../src/contracts/v1/error.js';
export class AutoHubFixtureV1 implements AutoHubDispatchClient{readonly fixtureVersion='fixture-v1';async startRun(i:StartRunV1,_s:AbortSignal):Promise<AutoHubRunV1>{if(i.fixtureScenario==='provider-rejected')throw new SafeExecutionError({family:'provider-rejected',code:'ZX_PROVIDER_REJECTED',message:'fixture provider rejected',retryable:false,traceId:'fixture'});if(i.fixtureScenario==='timeout')throw new SafeExecutionError({family:'timeout',code:'ZX_PROVIDER_TIMEOUT',message:'fixture timeout',retryable:true,traceId:'fixture'});return{runRef:'run_fixture_0001',status:'running'}}async getRun(i:GetRunV1,_s:AbortSignal):Promise<AutoHubRunV1>{if(i.fixtureScenario==='unknown-run')return{runRef:i.runRef,status:'unknown'};return{runRef:i.runRef,status:'succeeded',safeOutputRef:'provider-output-fixture-0001'}}async cancelRun(i:CancelRunV1,_s:AbortSignal):Promise<AutoHubRunV1>{return{runRef:i.runRef,status:'cancelled'}}}
