name: source-validation-bootstrap

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic validation paused during source correction commit sequence."
